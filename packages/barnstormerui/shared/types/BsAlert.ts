export enum BsAlertVariant {
  neutral = 'neutral',
  secondary = 'secondary',
  positive = 'positive',
  warning = 'warning',
  error = 'error',
}

export enum BsAlertType {
  temporary = 'temporary',
  persistent = 'persistent',
}
